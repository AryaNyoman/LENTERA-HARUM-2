# Salin file ini menjadi `.env` lalu isi nilainya. JANGAN commit .env.

# Dev lokal (SQLite). Di Railway diganti connection string Postgres.
DATABASE_URL="file:./dev.db"

# Kunci enkripsi PII anak di database website ini (AES-256-GCM, 32 byte hex).
# Buat dengan: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
KUNCI_SERVER=""

# ── Sinkron SIMPUS (Tahap 7 — boleh kosong dulu) ──
# Connection string Neon (read-only kalau bisa) milik SIMPUS petugas.
SIMPUS_DATABASE_URL=""
# Amplop layanan: DEK brankas SIMPUS terbungkus untuk job sinkron (disediakan pemilik).
SIMPUS_AMPLOP=""
